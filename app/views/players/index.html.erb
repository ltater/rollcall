<%= debug @players %>

<h1>Players</h1>

<%= link_to "New Player", new_player_path %>

<div id="players">
	<%= @players.each do |player| %>
	<div>
		<%= link_to player.name, player %>
	</div>
	<% end %>
</div>